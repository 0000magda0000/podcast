<%= simple_form_for @episode do |f| %>
  <%= f.input :title %>
  <%= f.input :audio, as: :file, input_html: { data: { direct_upload_url: rails_direct_uploads_url } } %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
